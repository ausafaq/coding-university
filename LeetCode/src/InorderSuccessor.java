public class InorderSuccessor {

    class Node {
        int data;
        Node left = null, right = null;

        Node(int data) {
            this.data = data;
        }
    }

    class BST {
        Node root;

        public Node insert(int key) {
            if(root == null) {
                return new Node(key);
            }

            if(key)
        }
    }
}
